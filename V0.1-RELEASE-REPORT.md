# EAS Visual v0.1 Experimental — Release Report

日期：2026-09-14。公开仓库：<https://github.com/FelixVollerei/eas-hmi>。

**Ready for public experimental release.**

本版已经把受控 POC 包装成真实可安装、可调用、可验证和可回退的 Agent Visual Tool。
这是 Experimental 发布结论，适用范围是人工/Agent 辅助 mask、整数平移和严格局部 patch；
不是自动分割产品、通用图片编辑质量保证，也没有宣称 Raster → 完整语义 SVG 已解决。

## 已实现

|交付|实现与边界|
|---|---|
|独立 Core|`src/eas_visual`；复用已有 EAS import/render/opaque_svg；`src/eas_hmi` 无源码改动|
|项目目录|不可变原始字节、RGBA 副本、64 色默认 baseline PNG/SVG；content-addressed 对象、mask、patch、history、output|
|Scene IR|baseline + 按需 promoted objects + ordered local repair/replacement layers；残余区域保留在 baseline|
|定位|正式支持 bbox、polygon、二值 mask；来源明确为 manual / Agent-assisted|
|结构编辑|整数 move，原对象像素直接复制；实际渲染后的对象 MAE 单独计算并要求为 0|
|局部模型编辑|prepare context → host model → external-patch → 显式 normalize → constrained composite → 实际渲染验证|
|Provider|ImageEditProvider 协议；mock、external-patch 文件交换适配器。模型调用由宿主执行；无指定模型 SDK 依赖|
|背景修复|donor/copy、显式 none、通过同一 patch 契约的 generative repair；估计背景均带 quality warning|
|可逆性|undo、redo、reset、checkout；新分支不删除旧历史；独立 replay 不调用模型|
|CLI|14 个命令；JSON 默认且 `--json` 明确为兼容别名；退出码 0 / 2 / 1；ASCII 转义保障 Windows 编码下 JSON 可解析|
|Agent|Python API + 6 个高层 stdio MCP 工具；工作目录允许范围；宿主无 Core 依赖|
|安装交付|wheel、源码、README / ROADMAP / FUTURE / CHANGELOG、示例项目、Demo 脚本、测试和 CI workflow|

### 完整性与 Fail Closed

所有操作先校验当前和非当前历史及其引用文件。失败不会移动 manifest 指针。
新资产先独占写入，再经 EAS SVG + Inkscape 原分辨率渲染和像素验证，最后原子替换 manifest。
原图、baseline 和历史资产不被写回覆盖。`render` 是导出已经验证的缓存，`replay` 才是独立重新渲染。

拒绝空/过大/非二值 mask、越界 target、与其它 promoted object 冲突、过期 provider request、
错误尺寸（除非显式 normalize）、透明 patch、损坏资产和未声明区域像素变化。
最大 4M 像素；单次 support ≤25%；margin 0–8 px；context 必须小于全图。
没有 `--allow-outside-change` override，当前发布始终严格拒绝。

文件摘要检测意外损坏，不是防止能够同时改写文件和摘要的攻击者；本机文件锁/原子替换也不等于
断电或网络文件系统持久性保证。失败可能留下未引用的中间文件，旧项目仍可用。

## 已验证

本机环境：Windows、Python 3.12.7、Inkscape 1.4.2；Pillow 12.3.0、NumPy 2.5.3、
Pydantic 2.13.5、MCP Python SDK 1.30.0。完整依赖快照见 `requirements-visual-tested.txt`。

- 第一次专项测试：**25 passed / 33.08 s**。
- 完整现有测试集：**270 passed / 127.96 s**，包含上述 25 项 Visual 测试和 245 项既有测试。
- 最终增加跨编码 Unicode JSON 回归后：**26 passed / 32.60 s**；CLI 和输出编码变化已重新覆盖。
- Ruff 对新 Core、CLI、MCP、Demo、测试检查通过。
- 独立虚拟环境安装 wheel，确认导入来自 `site-packages`，不依赖旧 editable 安装。
- wheel 环境 `eas-visual inspect`、旧 `eas-hmi --help` 正常；真实 MCP stdio 初始化并列出 6 工具。
- wheel 环境完整三个 Demo：**14 次 CLI 调用，19.67 s**；开发环境为 **19.98 s**。
  这是进程运行总耗时，不是 token/Agent 步数评测；模型调用为缓存重放，不计新模型耗时。

|关键测试|结果|
|---|---|
|baseline 文件不可变、promotion 不改变可见像素|通过|
|move 对象像素精确复制、原位置 donor 清除|通过|
|外部 patch 污染全部 context，最终 support 外稳定|通过，含 blending margin|
|undo / redo / checkout / reset / 分支 / reopen / replay|通过|
|provider 尺寸错误拒绝；显式 normalize 记录警告|通过|
|透明 patch、非二值 mask、错误 object ID / bbox / target|通过|
|损坏 history、patch、baseline、manifest、非当前分支 patch|通过，拒绝继续修改|
|renderer 人为改变 mask 外像素|通过，提交拒绝|
|manifest 提交前模拟磁盘失败|通过，旧 revision 保持完整|
|generative repair / 显式 none warning|通过；测试是确定性候选，不证明复杂修复质量|
|CLI JSON、MCP 真正 stdio 连接与越界路径拒绝|通过|

本轮没有出现 0xC0000005；**这不证明历史原生崩溃已修复**。
一次旧 CLI `--help` 被 PowerShell `Select-Object -First 3` 提前关闭管道而出现输出 OSError；
改用完整 capture 后同一已安装命令退出 0。这是检查方式导致的断管，不归因为历史访问违规。

## 三个 Demo 的实际结果

|Demo|support 占比|untouched MAE|untouched changed pixels|untouched max diff|额外结果|
|---|---:|---:|---:|---:|---|
|自制房间床移动|12.5%|0|0|0|对象 MAE=0；纯色地板 donor 可正确清除|
|Wikipe-tan 嘴部微笑|0.368468%|0|0|0|局部 8,589 px；真实缓存模型 patch|
|move → move → undo → undo → redo|按每次 move 声明|0|0|0|对应历史 hash 完全一致；4 个 scene states replay 成功|

可公开、可复现材料：

- [结构移动](docs/visual/structural.png)
- [模型原始返回 / accepted patch / 最终合成](docs/visual/generative-provider.png)
- [嘴部局部放大](docs/visual/generative-zoom.png)
- [Undo 序列](docs/visual/undo.png)
- [实际结构化指标](docs/visual/demo-metrics.json)
- [便携示例项目](examples/visual/room.eas)

初次 source-run evidence：`build/visual-demo-release/`；wheel-run evidence：`build/visual-wheel-demo/`。
这些本机 build 目录不进入 Git；使用 `python scripts/demo_visual.py --output build/new-demo` 可重建。
Git 中保留预览和指标；发布附件另提供完整 Demo 工程与图像。

### 生成式 Demo 的真实边界

使用上一阶段调用 image_gen 工具获得的局部返回。本轮 **0 次新模型调用**。
原始调用的 prompt、crop、返回图、工具名称均保留；工具未暴露准确 model ID，seed 未知，不伪造。
本轮把原始 context 像素与新建项目 crop 比较，完全相同后才重放。

模型返回 1409×1116，输入 context 480×380；显式 LANCZOS normalize 后，mouth mask 以外
context 有 **173,082 像素 / 98.8368%** 改变。最终仅回写预先声明的 mouth + 3 px margin，
全场景 support 外完全不变。肉眼确认微笑存在，皮肤色调与接缝仍有轻微变化。

模型调用、语义质量、风格和定位仍是 provider/Agent 的工作；本工具负责限制写入和验证。
外部宿主如果自行给模型全图，Core 无法阻止宿主另行调用；标准 prepare 契约只输出局部 crop。

## Experimental / Known limitations

1. 自动定位未做。人工/Agent bbox、polygon 和 mask 已正式支持；不会以自动 segmentation 为发布门槛。
2. donor 是估计，不恢复未知背景。复杂遮挡/纹理/边缘接缝仍是当前视觉质量瓶颈。
3. repair=none 会保留原对象，作为显式诊断选项发 warning，不冒充完成移除。
4. 已 promoted 对象的支持区域冲突会拒绝；未 promoted 残余对象可能被目标覆盖，暂无语义碰撞检测。
5. 只支持整数 move；swap、scale、rotate 和连续变换未实现。没有任意 renderer/缩放精确性承诺。
6. 每次打开审计全部引用文件，读成本随历史增大；每 revision 保存输出 PNG/SVG，尚无压缩/垃圾回收。
   **本版没有宣称解决此前历史规模读放大问题**，下一版本应量化并改进，不能偷偷撤掉完整性检查。
7. 标准 MCP 和 CLI 已验证；Codex / DeepSeek Harness / 墨小汐各宿主实机接入没有逐一认证。
8. 当前 adapter 是真实模型结果的文件交换，不是内置在线图像 API。Mock 只画确定性蓝色，不懂指令。
9. 旧 EAS-HMI 的偶发 Windows 原生崩溃调查仍开放。新实验通过不等于对整台机器稳定性作保证。
10. wheel 只包含软件；Demo 素材、脚本和示例项目来自 clone / 源码包 / 发布 Demo 附件。

## 未实现

没有 GUI、账户/云服务、训练模型、通用 ontology、整图自动 segmentation、完整 Photoshop 功能、
Codex plugin 包、专属 Harness adapter，也没有改 dsh-std 或墨小汐。详见 `FUTURE.md`。

## Release blockers

在上述 Experimental 范围内，**没有发现阻塞本次公开发布的严重问题**。
已验证的硬门槛是原始资产保留、显式 support、零未授权像素变化、可逆持久历史和干净安装。
未达到的通用图像质量和自动化能力已明确列为限制，不包装成已完成。

GitHub CI 的实际状态在最终发布记录中另记；本机测试通过不冒充远端 CI 成功。

## 推荐发布方式

保留现有 `FelixVollerei/eas-hmi` 仓库与 Git 历史，项目入口改为 EAS Visual。
使用 `eas-visual-v0.1.0` tag 创建 **GitHub Pre-release**；附 wheel、源码归档及完整可复现 Demo 包。
不发布为商业稳定版，不注册 PyPI 名称，不改仓库可见性之外的用户账户设置。

代码/自制房间 MIT；Kasuga 的 Wikipe-tan 及衍生 patch/预览单独按 CC BY-SA 3.0。
许可来源、变更说明和 attribution 见 `THIRD_PARTY.md`；授权不明的房地产户型图不进入公开素材。

下一阶段最有价值的是用真实用户选定的小对象改善 mask 与 repair 工作流，然后测量历史增长成本。
无需等待理想分割或理想图像模型才能收集 Agent Tool 使用反馈。

## 最终发布记录

- 本机验收通过，结构化记录：[verification.json](docs/visual/verification.json)。
- GitHub CI / tag / release 地址将在远端验证完成后更新。
