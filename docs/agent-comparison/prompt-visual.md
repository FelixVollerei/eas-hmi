你要完成一次合成 HMI 画面修改。Inkscape 已打开 `<visual-input.svg绝对路径>`，输出文件是 `<本组输出目录>/result.svg`。只能通过截图观察与鼠标、键盘、滚动操作编辑器；可以使用对象/图层面板、查找、变换、对齐、普通属性栏及编辑器帮助。不能调用 EAS-HMI、shell、Python、脚本、DOM/可访问性语义树、原始模型或文件读取 API；不能使用 XML/源码编辑器、扩展脚本或开发控制台；不能读取评分脚本、旧工作流或其他组文件。保存/另存为通过编辑器界面完成。

任务：在 cooling 页面，把 CHWP-01 至 CHWP-20 共 20 张泵卡片本体及其子图形，相对各自原位置右移 12、下移 8 个模型单位；只把 CHWP-07 卡片的宽度改为 164，保持高度不变；把页面主标题改成 `COOLING / REVIEWED`。其他对象、连接线位置、绑定、样式与其他页面保持原样。不要移动整个泵分组，因为其中还有不应移动的连接线。允许位置和宽度误差 ±0.5 模型单位。画布使用 SVG 用户单位，评估者已在预检时确认所用编辑器的单位与保存兼容性。

自行观察定位、执行、验证并另存为 result.svg。不要请求用户代做。若遇到不可恢复问题，保存可交付的当前结果并说明。预算为 20 分钟、120 次底层工具调用；一次包装调用内的多个 UI 动作分别记录，达到任一上限停止。

最后只报告：输出文件路径、已完成内容、尚未完成或异常，以及是否有人为介入。不要估算 token 数或声称通过你无权访问的评分器。
