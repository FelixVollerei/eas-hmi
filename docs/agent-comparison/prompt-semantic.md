你要完成一次合成 HMI 工程修改。工程目录是 `<semantic-project绝对路径>`，输出文件是 `<本组输出目录>/result.svg`。可以使用已安装的 `eas-hmi` CLI 及它的帮助文档查询和修改工程。只允许经 CLI 进行模型读写；可以用 shell 顺序调用命令或根据 CLI JSON 结果组合命令。不要直接读取/编辑 HEAD、提交库、原始模型、SVG XML、评分脚本、旧工作流或其他组文件。不要使用屏幕、鼠标或视觉识别。最终 SVG 必须通过 CLI render 产生。

任务：在 cooling 页面，把 CHWP-01 至 CHWP-20 共 20 张泵卡片本体及其子图形，相对各自原位置右移 12、下移 8 个模型单位；只把 CHWP-07 卡片的宽度改为 164，保持高度不变；把页面主标题改成 `COOLING / REVIEWED`。其他对象、连接线位置、绑定、样式与其他页面保持原样。不要移动整个泵分组，因为其中还有不应移动的连接线。允许位置和宽度误差 ±0.5 模型单位。

自行查询定位、执行、验证并导出 result.svg。不要请求用户代做。若遇到不可恢复问题，保存可交付的当前结果并说明。预算为 20 分钟、120 次底层工具调用；一次包装调用里的多个 CLI 命令分别计数，达到任一上限停止。

最后只报告：输出文件路径、已完成内容、尚未完成或异常，以及是否有人为介入。不要估算 token 数或声称通过你无权访问的评分器。
